name: myapp
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  sqflite: ^2.0.0+4
  path: ^1.9.1 # Change this line
  table_calendar: ^3.0.0
  fl_chart: ^1.0.0
  flutter_local_notifications: ^9.1.0 # Added for local notifications
  timezone: ^0.8.0 # Added for timezone support in notifications
  flutter_native_timezone: ^2.0.0 # Added for local timezone detection


dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
